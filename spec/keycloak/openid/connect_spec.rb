RSpec.describe Keycloak::Openid::Connect do
  it "has a version number" do
    expect(Keycloak::Openid::Connect::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
